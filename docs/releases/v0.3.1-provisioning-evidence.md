# v0.3.1 provisioning validation evidence

**Status:** Release preparation only. **Not published.**

This file is **excluded** from the packaged workspace snapshot (`workspace-snapshot-policy.ts` forbids `docs/releases/v0.3.1-provisioning-evidence.md`) so post-test evidence updates do not change `snapshotSha256` or `gitRootTreeSha1`. A new commit still changes manifest `sourceCommit` and therefore `snapshotContentId`; live-gate tarballs must be built from the exact tested commit.

## Snapshot identity distinction

| Field | Changes when evidence doc is committed? |
|-------|----------------------------------------|
| `snapshotSha256` | No (evidence file excluded from snapshot) |
| `gitRootTreeSha1` | No |
| `sourceCommit` | Yes (new commit) |
| `snapshotContentId` | Yes (includes `sourceCommit`) |

## Superseded pre-repair tarballs

| Tarball | Status |
|---------|--------|
| `245294d` (`/Users/Shared/p-dev-245294d.tgz`) | **Superseded** — marker commit replaced snapshot tree; hand-rolled tree SHA |
| `85ce12f` | **Superseded** — pre-correctness-repair CI green only |

Do not use pre-repair release evidence for final review.

## Replacement tarball (correctness repair, live-gate target)

| Field | Value |
|-------|-------|
| Branch | `release/v0.3.1-immutable-workspace-snapshot` |
| Implementation HEAD | `e00d865189beb64a00473bd4a40063fa895bf06c` |
| Draft PR | https://github.com/weston-uribe/agentic-product-development-harness/pull/60 |
| Tarball path (local) | `packages/p-dev/p-dev-harness-0.3.1.tgz` |
| Tarball copy | `/Users/Shared/p-dev-e00d865.tgz` |
| Tarball bytes | 2,324,912 |
| Tarball SHA-1 | `9d3bdf288951c7bc81645a7ee0424ef1dd778f6f` |
| Tarball SHA-256 | `b9baa92cde345ed4fe9360e4454fad9ef3507c74d39738e053439484c788644b` |
| Manifest `sourceCommit` | `e00d865189beb64a00473bd4a40063fa895bf06c` |
| Manifest `snapshotContentId` | `98821313a0b5ab55c66a582f828a234f8394e7d7383e1bfd1308f57c39196f54` |
| Manifest `snapshotSha256` | `53c5272505709e234e255fe5dd412a3dd33519f2932b32f2597a9bbd92f53bbc` |
| Manifest `gitRootTreeSha1` | `8f70d5c8b87112ace8ceae13ace6487cf4f507ca` |
| Manifest file count | 601 |
| Package inspect entry count | 1,718 (includes launcher/GUI assets) |
| Snapshot source ref | Clean `HEAD` at package-prepare time (no stale `P_DEV_SNAPSHOT_SOURCE_REF`) |
| Generated at (UTC) | 2026-07-14T03:02Z |

## Local validation (2026-07-14, repair)

| Command | Result |
|---------|--------|
| `npm ci` | Pass |
| `npm run build` | Pass |
| `npm test` | Pass — 970/970 |
| `npm run test:webhook` | Pass |
| `npm run package:p-dev:prepare` | Pass (clean HEAD) |
| `npm run package:p-dev:pack` | Pass |
| `npm run package:p-dev:inspect` | Pass — workspace snapshot + manifest present |

Focused suites: `git-object-plumbing`, `workspace-snapshot-validation`, `workspace-snapshot-digest`, `harness-snapshot-marker-tree`, `harness-repo-provisioning`, packaged provisioning GUI routes.

## Correctness repairs (8 blocking findings)

1. **Marker commit** — `base_tree` overlay on snapshot root tree; marker HEAD retains full workspace + `.harness/p-dev-managed-repo.json`.
2. **Root-tree SHA** — delegated to `git mktree` / `git write-tree`; UTF-8 bytewise path ordering via `Buffer.compare`.
3. **Embedded validation** — fail-closed `validateEmbeddedSnapshotFiles` before any GitHub mutation.
4. **Checkpoints** — awaited `onCheckpoint` persists repository ID and commit SHAs after each remote phase.
5. **Resume identity** — `verifyPendingRepositoryIdentity` before resumed mutations; repository ID mismatch fails closed.
6. **Reconciliation** — operation-specific repo description, deterministic commit metadata, ref fast-forward checks.
7. **Package provenance** — `prepare-p-dev-package.ts` requires clean HEAD; ignores stale `P_DEV_SNAPSHOT_SOURCE_REF`.
8. **Evidence claims** — this doc corrected; `245294d` tarball superseded.

## Automated provisioning-path evidence (mocked GitHub)

- `tests/setup/harness-snapshot-marker-tree.test.ts` — marker HEAD contains README + nested snapshot files; parent is snapshot commit; tree differs only by marker overlay.
- `tests/p-dev/git-object-plumbing.test.ts` — computed trees equal `git write-tree` oracle.
- `tests/p-dev/workspace-snapshot-validation.test.ts` — tampering matrix (bytes, size, SHA-256, blob SHA, root tree, mode, type, duplicate path, generation version).
- `tests/setup/harness-repo-provisioning.test.ts` — resume after marker failure, repository ID mismatch (zero mutations), snapshot-tampered preflight (zero mutations), legacy reconnect.

## Controlled real-account fresh provisioning gate

**Status: BLOCKED — dedicated `pdevtest` GitHub token not available in agent environment.**

| Precondition | Status |
|--------------|--------|
| Replacement tarball from `e00d865` | Verified locally |
| Manifest `sourceCommit` equals HEAD | Verified |
| Dedicated `pdevtest` identity (id `88105742`) | Documented |
| `PDEVTEST_GITHUB_TOKEN` or `gh auth token --user pdevtest` | **Unavailable** — only `weston-uribe` logged in via `gh`; no separate pdevtest account in `~/.config/gh` |
| Primary production identity | Available — **not used** for destructive fresh provisioning |

**Blocker:** Operator must supply a classic PAT with `repo` + `workflow` scopes for the `pdevtest` account.

### Live gate results

_Pending operator-owned rerun with dedicated test credentials._

## Explicit non-actions

- No npm publish
- No git tag or GitHub release
- No merge of PR #60
- No changes to `weston-uribe/p-dev-harness-template`
