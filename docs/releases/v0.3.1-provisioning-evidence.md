# v0.3.1 provisioning validation evidence

**Status:** Release preparation only. **Not published.**

This file is **excluded** from the packaged workspace snapshot (`workspace-snapshot-policy.ts` forbids `docs/releases/v0.3.1-provisioning-evidence.md`) so post-test evidence updates do not change `snapshotContentId` or require repeating the live provisioning gate.

## Implementation commit and tarball (live-gate target)

| Field | Value |
|-------|-------|
| Branch | `release/v0.3.1-immutable-workspace-snapshot` |
| Implementation HEAD | `245294d0ad6d21501dbda86a794aa0c16ea14a53` |
| Draft PR | https://github.com/weston-uribe/agentic-product-development-harness/pull/60 |
| Tarball path (local) | `packages/p-dev/p-dev-harness-0.3.1.tgz` |
| Tarball copy | `/Users/Shared/p-dev-245294d.tgz` |
| Tarball bytes | 2,302,000 |
| Tarball SHA-1 | `52c3012541aa5aaea7a810c0c6361a6d7254064d` |
| Tarball SHA-256 | `e72319be3a9a22ac4b39b750be903893c2b0bf506bc8aab7666636ef52aef504` |
| Manifest `sourceCommit` | `245294d0ad6d21501dbda86a794aa0c16ea14a53` |
| Manifest `snapshotContentId` | `75d07f2d5822ec5a5579effd3360cef487515be5c6e2576f36dc735a64f88f39` |
| Manifest `snapshotSha256` | `e9266c040eb96bd6bf28a38595647ff2337739f2763c3638e618ff1e9407b5ce` |
| Manifest `gitRootTreeSha1` | `928c504b89f72ecfb6a6978e0fcf22ac3837ac03` |
| Manifest file count | 595 |
| Package inspect entry count | 1,696 (includes launcher/GUI assets) |
| Snapshot source ref mechanism | `P_DEV_SNAPSHOT_SOURCE_REF=245294d0ad6d21501dbda86a794aa0c16ea14a53` |
| Generated at (UTC) | 2026-07-14T02:35Z |

## Local validation (2026-07-14)

| Command | Result |
|---------|--------|
| `npm ci` | Pass |
| `npm run build` | Pass |
| `npm test` | Pass — 965/965 |
| `npm run test:webhook` | Pass — 89/89 |
| `npm run package:p-dev:prepare` | Pass (explicit `P_DEV_SNAPSHOT_SOURCE_REF`) |
| `npm run package:p-dev:pack` | Pass |
| `npm run package:p-dev:inspect` | Pass — workspace snapshot + manifest present |

Focused suites exercised during repair: `workspace-snapshot-policy`, `workspace-snapshot-digest`, `workspace-snapshot-generator`, `package-packed-artifact`, `harness-repo-provisioning`, `harness-provisioning-pending-state`, `harness-provisioning-context`, packaged provisioning GUI routes, planner/implementation prompt contracts.

## GitHub CI (PR #60, HEAD `245294d`)

| Check | Result |
|-------|--------|
| `test` | Pass |
| `Analyze (javascript-typescript)` | Pass |
| `CodeQL` | Pass (after delegating blob hashing to `git hash-object`) |
| `Vercel` | Pass |
| `Vercel Preview Comments` | Pass |

## Automated provisioning-path evidence (mocked GitHub)

From `tests/setup/harness-repo-provisioning.test.ts` (Vitest, mocked provider):

- Fresh packaged path calls `createUserRepository`, **not** `createRepositoryFromTemplate`.
- Snapshot commit + marker v3 finalization verified in mock git store.
- Pending-state resume after partial blob upload / marker write failure.
- Legacy v0.3.0 managed marker reconnect without snapshot digest match requirement.
- Legacy marker finalization resume without repository recreation.
- Rename recovery when numeric `repositoryId` unchanged.
- Rejection of legacy marker missing `repositoryId` and legacy public template source.

## Controlled real-account fresh provisioning gate

**Status: BLOCKED — dedicated test account token unavailable in agent environment.**

| Precondition | Status |
|--------------|--------|
| Tarball built from implementation HEAD `245294d` | Verified locally |
| Manifest `sourceCommit` equals HEAD | Verified |
| Dedicated non-production test identity | `pdevtest` GitHub user exists (id `88105742`) |
| `PDEVTEST_GITHUB_TOKEN` or equivalent | **Not set** in agent shell |
| Primary production identity (`weston-uribe`) | Available via `gh` — **not used** for destructive fresh provisioning per release gate policy |

**Blocker:** Operator must supply a classic PAT with `repo` + `workflow` scopes for the `pdevtest` account (or another approved dedicated test identity). The agent cannot run the destructive fresh `p-dev-harness` creation gate without that credential.

**Operator rerun checklist (when token available):**

1. Archive any existing `pdevtest/p-dev-harness` to `p-dev-harness-v030-archive-YYYYMMDD-HHMMSS` (rename only).
2. Fresh temp directory + fresh `P_DEV_HOME`.
3. Install `/Users/Shared/p-dev-245294d.tgz` (or rebuild from `245294d`).
4. Complete Configure Step 1 fresh private workspace provisioning.
5. Verify marker v3 provenance, snapshot tree SHA, no template API access.
6. Append redacted results below.

### Live gate results

_Pending operator-owned rerun with dedicated test credentials._

## Legacy v0.3.0 reconnect

Preserved in implementation; validated by unit tests above. Existing managed workspaces with valid v0.3.0 markers reconnect without requiring v0.3.1 snapshot match and without rewriting repository contents.

## Explicit non-actions

- npm publish: **not performed**
- git tags: **not created**
- GitHub release: **not created**
- PR merge: **not performed**
- External template `weston-uribe/p-dev-harness-template`: **not mutated**
